import React from "react"
import { ProjectsWrapper } from "../elements"
import { ProjectCard } from "../components"

export const Projects = ({ data }) => {
  return <ProjectsWrapper></ProjectsWrapper>
}
